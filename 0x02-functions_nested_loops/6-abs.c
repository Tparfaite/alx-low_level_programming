#include "main.h"
/**
 * _abs - function that computes the absolute value of ineger
 *
 * @n: integer to be computed
 *
 * Return: Absolute value
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}

